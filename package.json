{
  "name": "vendor-analytics-platform",
  "version": "1.0.0",
  "description": "Unified launcher for Vendor Analytics & Retail Intelligence Platform",
  "scripts": {
    "start": "concurrently -n \"BACKEND,AI-SERVICE,FRONTEND\" -c \"blue.bold,magenta.bold,cyan.bold\" \"cd backend && npm start\" \"cd ai_service && uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "dev": "concurrently -n \"BACKEND,AI-SERVICE,FRONTEND\" -c \"blue.bold,magenta.bold,cyan.bold\" \"cd backend && npm start\" \"cd ai_service && uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.1",
    "helmet": "^7.1.0",
    "joi": "^17.13.1",
    "multer": "^1.4.5-lts.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
